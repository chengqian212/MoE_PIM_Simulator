# MoE-PIM Simulator Update — Prefill / Decode 分阶段调度与评估

**更新时间：2026-08-19**

本次更新主要重构第四步 Mapping & Scheduling 之后的推理性能评估流程。原有流程会将 Trace 中所有有效 segment 的 Token 直接展平，再统一按单 Token 调度，因此虽然能得到整体 cycles/token，但无法严格区分真实推理中的 Prefill 与 Decode。

本次更新重新检查 Chinese-SimpleQA Trace 的 segment 结构，建立了独立的 Prefill workload / scheduler / evaluator 与 Decode workload / exact evaluator / fast evaluator，并增加统一汇总、一键运行入口和回归测试。当前目标不是做消融，而是先得到语义正确、可复现、可继续扩展的正式 Baseline。

---

## 1. 本次主要解决的问题

### 1.1 原有评估口径不够准确

原 `trace_workload.py` 会遍历 JSON 中所有有效 segment，并把其中 Token 全部展平。这样会把：

```text
segment0 的 Prompt Token
+
segment1+ 的 Decode Token
```

混在一起按单 Token 调度。

因此旧的 `workload_evaluator.py`、`latency_analyzer.py` 等结果只能视为早期统一 Token 调试 Baseline，不能再解释为纯 Prefill 或纯 Decode。

旧结果约为：

```text
Mean ≈ 516 cycles/token
```

该结果保留作历史参考，不再作为正式性能指标。

---

## 2. Trace 结构重新确认

重新扫描全部 Chinese-SimpleQA Trace 后确认，2020 个 JSON 全部满足：

```text
segment0：多个 Token
segment1：1 Token
segment2：1 Token
segment3：1 Token
...
```

完整统计：

```text
Canonical [N>1, 1, 1, ...]：2020
Singleton-only：0
Non-canonical：0
```

Prefill `segment0` Token 数：

```text
Min   = 3
Mean  = 14.6827
P50   = 13
P95   = 25
P99   = 32
Max   = 70
```

长度分桶：

```text
2–16    : 1410
17–32   : 590
33–64   : 19
65–128  : 1
```

因此正式阶段划分为：

```text
segment0  -> Prefill
segment1+ -> Decode
```

最终数据量：

```text
Prefill Batches      = 2020
Prefill Input Tokens = 29659
Decode Tokens        = 255710
Total Trace Tokens   = 285369
```

满足：

```text
29659 + 255710 = 285369
```

---

## 3. 底层硬件与调度语义保持不变

本次没有修改已确定的硬件约束。

模型：

```text
58 MoE Layers
256 Routed Experts / Layer
1 Shared Expert / Layer
Top-8 Routing
```

每个 Token 每层实际执行：

```text
8 Routed Experts + 1 Shared Expert = 9 Experts
```

每个 Expert：

```text
gate / up / down
```

因此单 Decode Token 总任务数：

```text
58 × 9 × 3 = 1566 Tasks
```

硬件规则：

```text
不同 Sub-Cube：完全并行
同一 Sub-Cube：同一时间只允许一个 Weight-Cube 执行
Weight-Cube compute：1 cycle
Weight-Cube activation / switch：1 cycle
Cross Sub-Cube overhead：0 cycle
```

依赖：

```text
gate ─┐
      ├─> down
up   ─┘
```

即：

```text
down_ready_time = max(gate_finish_time, up_finish_time)
```

---

## 4. Prefill 调度语义

Prefill 不再把 Prompt 中每个 Token 分别跑完 58 层，而是按真实 Batch 方式：

```text
L0(all tokens)
→ L1(all tokens)
→ ...
→ L57(all tokens)
```

而不是：

```text
Token0 L0~L57
→ Token1 L0~L57
→ ...
```

同一 Layer 内：

- 所有 Token 的 gate / up 初始 ready；
- 某个 Token / Expert 的 down 不等待整个 Batch；
- 只要该 Token / Expert 自己的 gate 和 up 完成，并且目标 SC 可用，就可以执行 down；
- 同一 WC 连续服务多个 Token 时，第一次付 activation/switch，后续只付 compute。

例如同一个 WC 连续服务 30 个 Token：

```text
1 switch + 30 compute = 31 cycles
```

---

## 5. Decode 调度语义

Decode 只读取：

```text
segment1+
```

并要求每个 Decode segment 都是 singleton。

每个 Decode Token：

```text
58 Layers × 9 Experts × 3 Matrices = 1566 Tasks
```

---

## 6. Decode 连续状态修正

旧 `decode_evaluator.py` 每个 Token 都使用：

```text
initial_active_cube_by_subcube = None
```

等价于每个 Decode Token 都冷启动。

本次改为正式连续请求语义：

```text
一个 JSON = 一个 Request

Prefill
  ↓ final active WC state
Decode 1
  ↓
Decode 2
  ↓
Decode 3
  ↓
...
```

不同 JSON 之间重新开始。

同时保留：

```bash
--cold-start-each-token
```

用于回归旧行为。

### 连续状态验证

Cold Start：

```text
Mean Decode Cycles / Token = 534.60
Switches / Token           = 1550
Initial Activations        = 16
```

Continuous Request State：

```text
Mean Decode Cycles / Token = 534.60
Switches / Token           = 1566
Initial Activations        = 0
```

这是合理的，因为前一个 Token 最终停在 L57 的 WC，而下一个 Token 必须从 L0 开始；不同 Layer 的 WC 不同，所以仍需切换。由于 initial activation 与 switch 均为 1 cycle，因此最终 latency 不变。

正式 evaluator 仍保留 continuous request state，因为它更符合真实请求执行过程。

---

## 7. Prefill 正式全量结果

```text
Prefill Batches     = 2020
Total Input Tokens  = 29659
```

Prompt Length：

```text
Min   = 3
Mean  = 14.68
P50   = 13
P95   = 25
P99   = 32
Max   = 70
```

MoE Prefill Latency：

```text
Min   = 999 cycles
Mean  = 3308.27 cycles
P50   = 3085.50 cycles
P95   = 5184.30 cycles
P99   = 6445.40 cycles
Max   = 12505 cycles
```

Cycles / Input Token：

```text
Min   = 175.7258
Mean  = 231.2891
P50   = 230.3846
P95   = 260.0000
P99   = 272.3709
Max   = 333.0000
```

全局加权：

```text
Sum Cycles / Sum Input Tokens = 225.3183 cycles/input-token
```

Prompt Length 与 Prefill Latency Pearson：

```text
0.9968
```

---

## 8. Decode Fast Evaluator

为避免 255710 个 Decode Token 全部构造完整 task/event 对象，本次增加轻量 Fast Decode Scheduler。

Fast 版仍保留：

```text
gate / up / down dependency
SC 间并行
SC 内串行
ready-time 调度
route rank
matrix priority
WC switch / compute 周期
```

但减少 Python 对象创建和详细事件记录。

### FAST == EXACT 校验

默认前 100 个真实 Decode Token 同时运行：

```text
FAST Scheduler
vs
EXACT token_scheduler
```

校验：

```text
1. Total Token Cycles
2. L0 ~ L57 每层 Layer Cycles
```

只要任意一个不一致立即报错。

正式全量：

```text
FAST == EXACT Checked Tokens = 100
```

全部通过。

---

## 9. Decode 正式全量结果

```text
Pure Decode Tokens = 255710
Non-singleton Decode Segments = 0
```

MoE Decode Cycles / Token：

```text
Min   = 362
Mean  = 517.62
P50   = 526
P95   = 560
P99   = 572
Max   = 614
```

当前正式 Decode Baseline：

```text
517.62 cycles/token
```

平均最慢 Layer：

```text
L48 = 9.08
L46 = 9.08
L45 = 9.07
L42 = 9.06
L44 = 9.05
L41 = 9.05
L53 = 9.05
L47 = 9.05
L51 = 9.04
L54 = 9.03
```

Top Critical Sub-Cubes：

```text
SC-10  13.05%
SC-14  13.04%
SC-3   12.97%
SC-13  12.84%
SC-6   12.83%
SC-9   12.79%
SC-7   12.77%
SC-4   12.70%
SC-5   12.65%
SC-8   12.63%
```

长期分布整体较均衡，没有明显单 Layer 或单 SC 长期热点。

---

## 10. 本次新增 / 修改文件

### `scheduling/prefill_workload.py` — NEW

作用：保留 segment 边界并读取真实 Prefill `segment0`。

主要负责：

- segment0 提取；
- Token × Layer × Top-8 route 组织；
- Prefill Batch 长度统计；
- Trace stage 分类；
- 完整结构检查。

---

### `scheduling/prefill_layer_scheduler.py` — NEW

作用：调度一个 Prefill Batch 在单个 MoE Layer 中的执行。

支持：

- 多 Token gate/up 同时 ready；
- token/expert 粒度 down dependency；
- SC 间并行；
- SC 内串行；
- current active WC reuse；
- ready-time / route rank / token index / matrix priority。

已验证 B=1 时与原 `layer_scheduler.py` 完全一致。

---

### `scheduling/prefill_scheduler.py` — NEW

作用：调度完整 Prefill Batch 经过 58 个 MoE Layer。

```text
L0 whole batch
→ ...
→ L57 whole batch
```

并将每层 final active state 传给下一层。

已验证 B=1 时与原 `token_scheduler.py` 完全一致。

---

### `scheduling/prefill_evaluator.py` — NEW

作用：真实 segment0 Prefill 全量评估。

输出：

- Prompt Length；
- Prefill latency；
- cycles/input-token；
- weighted cycles/input-token；
- switches / wait；
- Layer statistics；
- Critical SC；
- Category；
- Prompt Length vs Latency Pearson。

结果：

```text
results/prefill/prefill_evaluation.json
```

---

### `scheduling/decode_workload.py` — NEW

作用：只读取纯 Decode workload。

规则：

```text
跳过 segment0
只读取 segment1+
严格要求 singleton
```

---

### `scheduling/decode_evaluator.py` — UPDATED

作用：Exact Decode Evaluator。

主要改动：

```text
旧：每 Token 冷启动
新：Prefill final state → Decode1 → Decode2 → ...
```

新增：

```bash
--cold-start-each-token
```

当前主要用于小规模 exact 验证和 Fast correctness reference，不建议用于 25 万 Token 全量。

---

### `scheduling/decode_fast_evaluator.py` — NEW

作用：快速完成全部纯 Decode Token 评估。

特点：

- 轻量 Layer event scheduler；
- route cache；
- 默认前 100 Token FAST == EXACT；
- 不一致立即停止；
- 支持全量 255710 Decode Token。

结果：

```text
results/decode/decode_fast_evaluation.json
```

---

### `scheduling/phase_evaluation_summary.py` — NEW

作用：统一 Prefill + Decode 正式结果。

输入：

```text
results/prefill/prefill_evaluation.json
results/decode/decode_fast_evaluation.json
```

输出：

```text
results/phase_evaluation_summary.json
```

后续 WebUI / 实验对比可统一读取这一份。

---

### `scheduling/run_phase_evaluation.py` — NEW

作用：一键运行完整阶段评估。

正式全量：

```bash
python -m scheduling.run_phase_evaluation
```

快速 Smoke：

```bash
python -m scheduling.run_phase_evaluation --smoke
```

已有结果复用：

```bash
python -m scheduling.run_phase_evaluation --reuse-existing
```

---

### `tests/test_phase_evaluation.py` — NEW

作用：固定本次阶段语义和正确性。

测试内容：

```text
1. Prefill B=1 == token_scheduler
2. Prefill Tasks = B × 58 × 9 × 3
3. Decode workload 不包含 segment0
4. Decode Token Tasks = 1566
5. Fast Decode == Exact Decode
6. Phase Summary 能正确解析两阶段结果
7. 未 validated 的 Decode Scheduler 不能作为正式 Summary
```

当前全部 PASS。

---

## 11. 继续复用、没有失效的核心文件

### `scheduling/runtime_index.py`

继续负责：

- expert / matrix → physical WC 查询；
- Top-K validation；
- Shared Expert 加入；
- gate/up/down mapping；
- SC / cube 定位。

### `scheduling/layer_scheduler.py`

继续作为单 Token × 单 Layer 的 Exact Scheduler。

### `scheduling/token_scheduler.py`

继续作为单 Token × 58 Layer Exact Scheduler。

它本来就支持：

```text
initial_active_cube_by_subcube
final_active_cube_by_subcube
```

因此本次 Decode continuous state 不需要修改底层 scheduler。

它也是 Fast evaluator 的 correctness reference。

---

## 12. 哪些旧文件不再作为正式结果入口

需要区分“文件失效”和“文件仍能用，但不能再代表正式阶段指标”。目前主要是后者。

### `scheduling/trace_workload.py`

保留。

仍可作为通用 flattened workload reader，但因为会混合：

```text
segment0 + segment1+
```

所以不再用于正式 Prefill / Decode 分阶段评估。

正式使用：

```text
Prefill -> prefill_workload.py
Decode  -> decode_workload.py
```

### `workload_evaluator.py`

保留作：

- Scheduler smoke test；
- Mapping 调试；
- Legacy unified-token benchmark。

但不能再把输出解释为纯 Decode 或 Prefill。

### `latency_analyzer.py`

同样保留作旧 flattened workload 分析。

旧 `~516 cycles/token` 只视为 Legacy / Mixed Token Baseline。

### 旧版 `decode_evaluator.py`

已被当前连续状态版本替换。

旧冷启动行为通过：

```bash
--cold-start-each-token
```

保留。

---

## 13. 推荐目录结构

```text
MoE_PIM_Simulator/
│
├─ scheduling/
│  ├─ runtime_index.py
│  ├─ layer_scheduler.py
│  ├─ token_scheduler.py
│  ├─ trace_workload.py              # Legacy / generic flattened workload
│  │
│  ├─ prefill_workload.py            # NEW
│  ├─ prefill_layer_scheduler.py     # NEW
│  ├─ prefill_scheduler.py           # NEW
│  ├─ prefill_evaluator.py           # NEW
│  │
│  ├─ decode_workload.py             # NEW
│  ├─ decode_evaluator.py            # UPDATED
│  ├─ decode_fast_evaluator.py       # NEW
│  │
│  ├─ phase_evaluation_summary.py    # NEW
│  └─ run_phase_evaluation.py        # NEW
│
├─ tests/
│  └─ test_phase_evaluation.py       # NEW
│
└─ results/
   ├─ prefill/
   │  └─ prefill_evaluation.json
   ├─ decode/
   │  ├─ decode_evaluation.json
   │  └─ decode_fast_evaluation.json
   └─ phase_evaluation_summary.json
```

---

## 14. 当前推荐命令

回归测试：

```bash
python -m pytest tests/test_phase_evaluation.py -v
```

Prefill 全量：

```bash
python -m scheduling.prefill_evaluator
```

Decode Fast 全量：

```bash
python -m scheduling.decode_fast_evaluator --exact-check 100
```

一键完整评估：

```bash
python -m scheduling.run_phase_evaluation
```

已有结果只重新汇总：

```bash
python -m scheduling.run_phase_evaluation --reuse-existing
```

快速 Smoke：

```bash
python -m scheduling.run_phase_evaluation --smoke
```

---

## 15. 当前正式 Baseline

### Prefill

```text
Batches              = 2020
Input Tokens         = 29659
Mean Latency         = 3308.27 cycles
P50                  = 3085.50
P95                  = 5184.30
P99                  = 6445.40
Mean Cycles / Token  = 231.2891
Global Weighted      = 225.3183
```

### Decode

```text
Tokens                = 255710
Mean Cycles / Token   = 517.62
P50                   = 526
P95                   = 560
P99                   = 572
Max                   = 614
```

---

## 16. 指标命名注意

当前 simulator 只模拟 58 个 MoE Expert Layer，不包含：

- Attention；
- KV Cache；
- Embedding；
- LM Head；
- 其他非 MoE 模块。

因此正式使用：

```text
MoE Prefill Latency
MoE Expert Prefill Latency
MoE Decode Cycles / Token
MoE Expert Decode Cycles / Token
```

不要直接写：

```text
Full TTFT
Full TPOT
```

---

## 17. 本次更新后的完整评估链

```text
Trace
  ↓
Prefill / Decode Stage Split
  ↓
Prefill Workload
  ↓
Prefill Batch Scheduler
  ↓
Prefill Evaluator
  ↓
Prefill Result
```

以及：

```text
Trace
  ↓
Decode Workload
  ↓
Exact Token Scheduler
  ↓
Fast Decode Scheduler
  ↓
FAST == EXACT Validation
  ↓
Full Decode Evaluation
  ↓
Decode Result
```

最后：

```text
Prefill Result
      +
Decode Result
      ↓
Phase Evaluation Summary
```

并由：

```text
tests/test_phase_evaluation.py
```

提供回归保护。

---

## 18. 后续计划

当前暂不进行 Mapping 消融或多算法对比。

下一阶段：

1. 固定当前正式 Baseline；
2. 将 Mapping / Prefill / Decode 结果接入 WebUI；
3. 增加 Mapping、Layer、Sub-Cube、Token Scheduling 可视化；
4. 后续再增加新的 Mapping / Scheduling 算法；
5. 所有新算法统一使用当前 Trace、硬件配置和 evaluator 进行公平比较。

当前版本的核心目标是：

> 建立语义正确、可复现、可测试的 Prefill / Decode 正式评估基线，为后续算法优化、消融实验和可视化提供统一基础。

---

# Suggested Git Commit

```text
feat: add stage-aware Prefill/Decode scheduling and evaluation pipeline
```

或：

```text
feat: separate Prefill and Decode workloads with validated full-trace evaluation
```

Commit description：

```text
- split Chinese-SimpleQA traces into segment0 Prefill and segment1+ Decode
- add batch-aware Prefill workload and schedulers
- add exact Prefill evaluator
- add pure Decode workload reader
- support continuous request state in Decode evaluator
- add fast Decode scheduler with FAST==EXACT validation
- run full evaluation on 2020 Prefill batches and 255710 Decode tokens
- add unified phase evaluation summary
- add one-command phase evaluation runner
- add regression tests for stage semantics and scheduler consistency
- keep legacy flattened workload tools for debugging only
```
