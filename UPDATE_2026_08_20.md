# UPDATE 2026-08-20

## 本次更新

本次主要完成了 MoE Mapping 阶段的消融实验补充，并对 Prefill / Decode 性能进行了完整评估。

### 1. 新增 Sequential Pairing

在原有 Trace-aware Pairing 基础上增加 Sequential Pairing 作为基础对照。

Sequential Pairing 不使用 Trace 共激活信息，按 Expert ID 顺序进行 Routed up 配对：

- E0 + E1
- E2 + E3
- ...
- E254 + E255

原有 Trace-aware Pairing 保持不变，仍采用：

- 基于 Expert frequency / coactivation 的 Greedy Pairing
- Local Search Pair Swap

运行时可通过：

--pairing-mode trace_aware

或：

--pairing-mode sequential

进行切换。

---

### 2. 新增 Round-Robin Sub-Cube Mapping

在原有 Trace-aware Sub-Cube Mapping 基础上增加 Round-Robin Mapping。

Round-Robin Mapping 不使用 Expert frequency、coactivation 和 weighted load 选择 Sub-Cube，仅按照轮询方式分配 LogicalPlane，同时保留以下硬约束：

- 每个 Sub-Cube 的 Plane 数不能超过 D
- 同一个 Expert 的 gate 和 up 不能位于同一个 Sub-Cube

运行时可通过：

--mapping-mode trace_aware

或：

--mapping-mode round_robin

进行切换。

---

### 3. 完成四组 Mapping 消融实验

当前共形成四组实验：

| Experiment | Pairing | Mapping |
| --- | --- | --- |
| Naive | Sequential | Round-Robin |
| Pairing Only | Trace-aware | Round-Robin |
| Mapping Only | Sequential | Trace-aware |
| Full | Trace-aware | Trace-aware |

所有实验保持以下条件一致：

- N = 4
- H = 7168
- W = 4096
- D = 1398
- 16 个 Sub-Cube
- P = 22359
- Q = 22368
- 相同 Spatial Layout
- 相同 Chinese-SimpleQA Trace
- 相同 Prefill / Decode Scheduler

---

### 4. 完成四组 Prefill / Decode 全量评估

全量数据：

- Prefill Batches：2020
- Prefill Input Tokens：29659
- Decode Tokens：255710
- Decode Fast Scheduler 前 100 Token 均通过 FAST == EXACT 校验

核心结果：

| Experiment | Prefill Mean | Decode Mean | Decode P95 |
| --- | ---: | ---: | ---: |
| Naive | 4007.64 | 581.94 | 610 |
| Pairing Only | 4020.67 | 584.35 | 612 |
| Mapping Only | 3308.39 | 518.37 | 560 |
| Full | 3308.27 | 517.62 | 560 |

相对 Naive：

- Mapping Only：Prefill 降低约 17.45%，Decode 降低约 10.93%
- Full：Prefill 降低约 17.45%，Decode 降低约 11.05%

当前结果表明 Trace-aware Sub-Cube Mapping 是主要性能优化来源。

---

### 5. Pairing / Mapping 中间指标

Pairing Cost：

- Sequential：2977431
- Trace-aware：439156
- 降低约 85.25%

说明 Trace-aware Pairing 能明显减少共享同一 Plane 的两个 Routed up Expert 的共激活情况。

Mapping Conflict Cost：

- Naive：178650012
- Pairing Only：178107407
- Mapping Only：134175599
- Full：133349805

虽然 Pairing 能明显降低局部 Pairing Cost，但单独使用时没有明显转化为最终 Prefill / Decode 性能收益；整体性能主要由 Sub-Cube Mapping 决定。

---

### 6. 新增自动消融汇总

新增：

evaluation/ablation_summary.py

可通过：

python -m evaluation.ablation_summary

自动读取四组 Mapping 和 Phase Evaluation 结果，并生成：

esults/experiments/ablation_summary.json

自动汇总：

- Pairing Cost
- Mapping Conflict Cost
- Prefill Mean
- Prefill Cycles / Input Token
- Decode Mean
- Decode P95
- 相对 Naive 的性能提升比例

---

### 7. Git 数据管理

将：

deepseek_r1_trace/

加入 .gitignore。

Trace 数据仅保留在本地，不再提交到 Git 仓库。
